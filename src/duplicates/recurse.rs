// fn add_path(path: PathBuf, song_map: &mut HashMap<Hash, Vec<PathBuf>>) -> Result<(), crate::Error> {
//     if path.is_file() {
//         add_file(path, song_map)?;
//     } else if path.is_dir() {
//         add_dir(path, song_map)?;
//     }
//     Ok(())
// }
//

